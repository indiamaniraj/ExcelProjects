import { CustomPipes2Pipe } from './custom-pipes2.pipe';

describe('CustomPipes2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPipes2Pipe();
    expect(pipe).toBeTruthy();
  });
});
